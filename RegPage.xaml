<Page x:Class="salon_krasoti.RegPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:salon_krasoti"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegPage">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="loginText" Grid.Row="0" Margin="0,0,0,10" />
        <PasswordBox x:Name="passwordText" Grid.Row="1" Margin="0,0,0,10" />
        <TextBox x:Name="nameText" Grid.Row="2" Margin="0,0,0,10" />
        <TextBox x:Name="surnameText" Grid.Row="3" Margin="0,0,0,10" Text="Фамилия"/>
        <TextBox x:Name="emailText" Grid.Row="4" Margin="0,0,0,10" 
                 Text="Email"/>
        <TextBox x:Name="phoneText" Grid.Row="5" Margin="0,0,0,10" 
                 Text="Телефон"/>
        <Button Grid.Row="6" Content="Зарегистрироваться"  Click="signUpButton_Click"/>
        <Button Grid.Row="7" Content="Назад" Margin="0,10"  Click="NavigateToAuth_Click"/>
    </Grid>
</Page>
